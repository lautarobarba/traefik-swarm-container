api:
  dashboard: true
  # insecure: true

log:
  # level: INFO
  level: DEBUG

entryPoints:
  web:
    address: ":80"

    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https

  websecure:
    address: ":443"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false

  file:
    directory: /etc/traefik/dynamic
    watch: true

certificatesResolvers:
  letsencrypt:
    acme:
      email: lautaro.barba.94@gmail.com

      storage: /letsencrypt/acme.json

      httpChallenge:
        entryPoint: web
